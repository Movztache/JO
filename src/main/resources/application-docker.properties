# ================================
# CONFIGURATION DOCKER/AWS
# ================================

# Nom de l'application
spring.application.name=jeux-olympiques

# Configuration de la base de donnees (variables d'environnement)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/jeux_olympiques}
spring.datasource.username=${DATABASE_USERNAME:adminco}
spring.datasource.password=${DATABASE_PASSWORD:adminco}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuration JPA pour production
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuration JWT
app.jwt.secret=${JWT_SECRET:aJmA6#S6f!K5rbNRqBaRJsMrd&K6adeyNPpFM4Qr}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# Configuration du serveur
server.port=8080
server.servlet.context-path=/

# Configuration CORS pour production
cors.allowed.origins=${CORS_ORIGINS:http://localhost:4200,https://votre-domaine.com}

# Configuration des logs pour production
logging.level.root=INFO
logging.level.com.example.jeuxolympiques=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Actuator pour health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.db.enabled=true
